# Pickaxe GitHub Pages Final Fix

Upload the CONTENTS of this folder to the root of:
Burberrry/Pickaxe-Capital

This fixes GitHub Pages project-site paths:
- uses styles.css instead of /styles.css
- uses brand/... instead of /brand/...
- uses assets/... instead of /assets/...
- uses hash routes such as #/agents instead of /agents

After upload, open:
https://burberrry.github.io/Pickaxe-Capital/#/agents

Then hard refresh:
Command + Shift + R
